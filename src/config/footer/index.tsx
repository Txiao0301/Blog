import { Layout } from 'antd';

const { Footer } = Layout;

function MyFooter() { 
    return (
        <Footer style={{ textAlign: 'center' }}>Mala Rosie</Footer>
    );
};

export default MyFooter;